# Legacy Root Runners

These scripts used to live at the repository root.

They are preserved for reproducibility and provenance because older reports,
handoff notes, and command examples still depend on them.

New work should prefer maintained runners under `scripts/` or `scripts/slurm/`.
Do not delete these files unless the corresponding research reports and results
no longer depend on them.
